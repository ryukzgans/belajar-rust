// Pada tipe data enum,
// keyword pub cukup ditambahkan pada definisi enum type, tidak perlu ditambahkan satu persatu di tiap enum values.

pub enum Company {
    Apple,
    Microsoft,
    Google,
    Github,
}
